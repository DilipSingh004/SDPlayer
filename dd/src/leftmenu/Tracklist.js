import React from "react";
// import "../styles/equilizer.css";


function Tracklist() {
  return (

    <div>
     {/* <div className="equalizer">  
        {[...Array(20)].map((_, index) => (  
          <div key={index} className={`bar bar${index + 1}`}></div>  
        ))}  
      </div>   */}
    </div>
  );
}

export default Tracklist;